name: Release Build - Windows

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g., 0.2.2)'
        required: true
        default: '0.2.2'

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-tauri-windows:
    name: Build Windows (x64)
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: agentreplay-ui/package-lock.json

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: 'agentreplay-tauri -> target'
          cache-targets: true

      - name: Cache Cargo bin
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: ${{ runner.os }}-cargo-bin-tauri-2.0

      - name: Install frontend dependencies
        working-directory: agentreplay-ui
        run: npm ci

      - name: Build frontend
        working-directory: agentreplay-ui
        run: npm run build

      - name: Install Tauri CLI
        run: |
          if (!(Get-Command cargo-tauri -ErrorAction SilentlyContinue)) {
            cargo install tauri-cli --version "^2.0"
          }
        shell: pwsh

      - name: Build Tauri App
        working-directory: agentreplay-tauri
        run: cargo tauri build --target x86_64-pc-windows-msvc
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

      - name: Normalize Windows artifact names
        shell: bash
        run: |
          MSI_DIR="agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi"
          NSIS_DIR="agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis"

          EXE=$(ls -1 "$NSIS_DIR"/*.exe 2>/dev/null | head -1 || true)
          if [[ -n "$EXE" ]]; then
            mv "$EXE" "$NSIS_DIR/Agent.Replay.Alpha_x64-setup.exe"
          fi

          NSIS_ZIP=$(ls -1 "$NSIS_DIR"/*.nsis.zip 2>/dev/null | head -1 || true)
          if [[ -n "$NSIS_ZIP" ]]; then
            mv "$NSIS_ZIP" "$NSIS_DIR/Agent.Replay.Alpha_x64-setup.nsis.zip"
            if [[ -f "${NSIS_ZIP}.sig" ]]; then
              mv "${NSIS_ZIP}.sig" "$NSIS_DIR/Agent.Replay.Alpha_x64-setup.nsis.zip.sig"
            fi
          fi

          MSI=$(ls -1 "$MSI_DIR"/*.msi 2>/dev/null | head -1 || true)
          if [[ -n "$MSI" ]]; then
            mv "$MSI" "$MSI_DIR/Agent.Replay.Alpha_x64_en-US.msi"
          fi

          MSI_ZIP=$(ls -1 "$MSI_DIR"/*.msi.zip 2>/dev/null | head -1 || true)
          if [[ -n "$MSI_ZIP" ]]; then
            mv "$MSI_ZIP" "$MSI_DIR/Agent.Replay.Alpha_x64_en-US.msi.zip"
            if [[ -f "${MSI_ZIP}.sig" ]]; then
              mv "${MSI_ZIP}.sig" "$MSI_DIR/Agent.Replay.Alpha_x64_en-US.msi.zip.sig"
            fi
          fi

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Agentreplay-x86_64-pc-windows-msvc
          path: |
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/Agent.Replay.Alpha_x64_en-US.msi
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/Agent.Replay.Alpha_x64_en-US.msi.zip
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/Agent.Replay.Alpha_x64_en-US.msi.zip.sig
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Agent.Replay.Alpha_x64-setup.exe
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Agent.Replay.Alpha_x64-setup.nsis.zip
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Agent.Replay.Alpha_x64-setup.nsis.zip.sig
          if-no-files-found: error

      - name: Create Release & Upload
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && format('v{0}', github.event.inputs.version) || github.ref_name }}
          name: Agent Replay Alpha ${{ github.event_name == 'workflow_dispatch' && format('v{0}', github.event.inputs.version) || github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name || github.event.inputs.version, 'alpha') || contains(github.ref_name || github.event.inputs.version, 'beta') }}
          overwrite_files: true
          generate_release_notes: true
          files: |
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/Agent.Replay.Alpha_x64_en-US.msi
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Agent.Replay.Alpha_x64-setup.exe
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Agent.Replay.Alpha_x64-setup.nsis.zip
            agentreplay-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Agent.Replay.Alpha_x64-setup.nsis.zip.sig
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
